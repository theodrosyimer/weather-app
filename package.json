{
  "name": "weather-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "install:latest": "ncu -u && pnpm install",
    "vercel:config": "vercel --local-config ./vercel.json",
    "vercel:git": "vercel git connect",
    "vercel": "npm run vercel:config && npm run vercel:git",
    "prod": "vercel --prod"
  },
  "devDependencies": {
    "typescript": "^5.0.4",
    "postcss": "^8.4.21",
    "postcss-preset-env": "^8.3.0",
    "vite": "^4.2.1"
  },
  "dependencies": {
    "@types/node": "^18.15.11"
  }
}
